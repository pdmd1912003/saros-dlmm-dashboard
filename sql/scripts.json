 pools (
    id,
    pool_address,
    token_base,
    token_quote,
    token_base_decimal,
    token_quote_decimal,
    trade_fee,
    base_reserve,
    quote_reserve,
    created_at,
    updated_at
  )
 position (
      id,
      position_address,
      wallet_id,
      pool_id,
      lower_bin_id,
      upper_bin_id,
      liquidity_shape,
      liquidity_shares,
      liquidity_amount,
      token_base_amount,
      token_quote_amount,
      is_active,
      created_at,
      updated_at
    )

wallets (
        id,
        owner_address,
        nickname,
        created_at,
        updated_at
      )
   